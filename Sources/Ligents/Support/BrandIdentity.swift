import SwiftUI

enum BrandIdentity {
    static let accent = Color(red: 0.15, green: 0.59, blue: 0.98)
    static let accentMuted = accent.opacity(0.16)
    static let accentSoft = accent.opacity(0.08)
    static let productDescriptor = "Quota switchboard"
    static let profileDescriptor = "Multi-account AI subscription limits"
}
